#include "main.h"
/**
 * _islower - use this code
 * @x: see as viraiable
 * Description: look for lower case
 * Return: 1 if x is lower case
 */
int _islower(int x) 
{
	return (x >= 'a' && x <= 'z');
}
